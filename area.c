// Program to print area of a rect
// Date : 07-JUL-2023

#include <stdio.h>

void main()
{
 int length, width, area;

      length = 10;
      width = 20;
      area = length * width;

      printf("Area of rect with length: %d, width: %d is %d",
             length, width, area);
}
